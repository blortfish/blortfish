{
  "name": "excludetube-ui",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "excludetube-ui/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project excludetube-ui --web",
  "targets": {
    "deploy": {
      "executor": "nx:run-commands",
      "dependsOn": [
        {
          "target": "build",
          "projects": "self"
        },
        {
          "target": "test",
          "projects": "self"
        }
      ],
      "options": {
        "command": "aws sso login --profile dflint && AWS_PROFILE=dflint aws s3 sync ./dist/excludetube-ui s3://excludetube-ui  --acl private"
      }
    }
  }
}
